[project]
name = "te_analysis"
version = "0.0.1-dev"
description = "CCDS wrapper over RiboBase/snakescale + CenikLab/TE_model"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
