backend_name: te_model
reference_backend_root: /home/xrx/my_project/te_analysis/raw_motheds/TE_model
method_boundary: downstream

input_contract:
  main_handoff_object: experiment_ribo_collection
  optional_aggregate_object: all_ribo
  required_sidecars:
    - study_manifest
    - pairing_reference
    - sample_selection_table
    - nonpolyA_reference
    - infor_filter_reference
    - downstream_run_config

stages:
  extraction: explicit_wrapper_stage
  winsorization: explicit_wrapper_stage
  filtering: explicit_wrapper_stage
  te_compute: explicit_wrapper_stage
  result_packaging: explicit_wrapper_stage
