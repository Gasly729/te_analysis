name: te-analysis
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - pandas>=2.0
  - pyyaml>=6.0
  # TODO(T0): confirm snakemake vs nextflow from vendor_contracts.md
  - snakemake
  # - nextflow
  - pip
  - pip:
    - pytest>=7.0
    - pysradb>=2.0
    # TE_model runtime deps (vendor/TE_model/src/*.py imports)
    - ribopy>=0.0.1
    - bioinfokit>=2.1
    - biopython>=1.80
